---
title: "Final Project"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

```{r}
# install.packages("arules")

library(arules)
data <- read.csv("AppleStore.csv")

# data_rules <- apriori(data)
# inspect(sort(data_rules, by = "lift")[1:30])
```

